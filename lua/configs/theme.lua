vim.cmd([[colorscheme tokyonight-night]])

colorscheme = tokyonight

-- local in_tmux = os.getenv("TMUX") ~= nil

-- require("tokyonight").setup({
-- 	style = moon,
-- 	transparent = not in_tmux,
-- })
